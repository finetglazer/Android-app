<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Title centered -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:fontFamily="@font/comici"
        android:text="Tourist Management System"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginBottom="20dp"/>

    <!-- Spinner -->
    <Spinner
        android:id="@+id/spinner"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:fontFamily="@font/comici"
        android:padding="12dp"
        android:textSize="16sp"
        android:textStyle="bold" />

    <!-- Itinerary EditText -->
    <EditText
        android:id="@+id/lich_trinh"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:hint="Itinerary"
        android:fontFamily="@font/comici"
        android:inputType="text"
        android:padding="12dp" />

    <!-- Time EditText -->
    <EditText
        android:id="@+id/thoi_gian"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:hint="Time"
        android:fontFamily="@font/comici"
        android:inputType="text"
        android:padding="12dp" />

    <!-- Search Button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"

        android:orientation="horizontal">
        <Button
        android:id="@+id/searchButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:fontFamily="@font/comici"
        android:padding="12dp"
        android:text="Add"
        android:textSize="16sp"
            android:layout_marginRight="20dp"
        android:textStyle="bold" />

        <Button
            android:id="@+id/UpdateButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:fontFamily="@font/comici"
            android:padding="12dp"
            android:text="Update"
            android:textSize="16sp"
            android:textStyle="bold" />

    </LinearLayout>

    <!-- RecyclerView to replace ScrollView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewTours"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:padding="16dp" />

</LinearLayout>
