<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.HomeAct"
            android:exported="true"
            android:theme="@style/Theme.MyApplication">
        </activity>

        <activity
            android:name=".activity.AddAct"
            android:exported="true"
            android:theme="@style/Theme.MyApplication">
        </activity>

        <activity
            android:name=".dao.MyActivity"
            android:exported="true"
            android:theme="@style/Theme.MyApplication">
        </activity>

        <activity
            android:name=".activity.AddEditUserActivity"
            android:exported="true"
            android:theme="@style/Theme.MyApplication">
        </activity>

        <activity
            android:name=".activity.UserCrudActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light"> <!-- Make sure this is AppCompat-based -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.AddCatAct"
            android:exported="true"
            android:theme="@style/Theme.MyApplication">
        </activity>

        <activity android:name=".test2.MainActivity"

            android:exported="true"
            android:theme="@style/Theme.MyApplication">

            <intent-filter
                android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



    </application>

</manifest>